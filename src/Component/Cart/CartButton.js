import React from 'react';

import classes from './CartButton.module.css'
const CartButton = () => {
  return (
    <>
     <button className={classes.cart_btn}>See Cart</button> 
    </>
  );
}

export default CartButton;
